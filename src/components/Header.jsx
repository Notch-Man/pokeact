import React from "react";
import { Container } from "react-bootstrap";

const Header = () => {
  return (
    <Container fluid class="mt-4">
      <h1 className="text-center" style={{ fontSize: "60pt" }}>
        Pokeact
      </h1>
    </Container>
  );
};

export default Header;
